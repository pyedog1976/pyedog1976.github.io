---
description: 在本仓库完成代码/内容修改后自动提交并推送到 GitHub
alwaysApply: true
---

# 完成后推送到 GitHub

在本项目（`pyedog1976.github.io`）中，**当你对仓库文件做了实质性修改并认为任务已完成时**，在回复用户之前应执行：

1. `git status` 确认有改动；若仅格式化/无意义 diff 且用户未要求提交，可跳过。
2. `git add` 相关文件（勿加入 `.env`、密钥或未跟踪的私有大文件）。
3. `git commit -m "…"`：一句英文或中文说明，说明改了什么、为何改。
4. `git push` 到当前分支跟踪的远程（一般为 `origin master`）；若推送失败，根据错误重试或说明原因，**不要**假定已成功。

**不推送的情况**：用户明确只要本地修改、只读问答、或用户说不要提交/不要 push。

默认分支为 `master` 时，可用：`git push origin HEAD`。
